{
  "name": "nesi-brainrot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:yandex": "YANDEX_BUILD=1 vite build --outDir dist-yandex",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "cannon-es": "0.20.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.4"
  }
}
